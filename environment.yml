name: unstructured

channels:
  - defaults
  - anaconda
  - conda-forge
  - pytorch

dependencies:
  - python=3.10
  - pytorch=2.1.2
  - pywin32
  - torchvision
  - pip
  - pip:
    - huggingface-hub
    - layoutparser
